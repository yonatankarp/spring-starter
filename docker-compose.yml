services:
  postgres:
    container_name: kotlin-spring-boot-template-postgres
    networks:
      - proxynet
    image: postgres:18-alpine
    restart: always
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "secret"
      POSTGRES_DB: "kotlin-spring-boot-template"
    ports:
      - "5432:5432"
    expose:
      - "5432"

networks:
  proxynet:
    name: kotlin-spring-boot-template-network
